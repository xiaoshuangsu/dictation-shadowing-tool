# V3 极简数据留存系统

---

# 一、系统目标

在只有 Dictation 和 Shadowing 两个功能的情况下，
通过最少的数据结构，实现最大留存效果。

核心目标：

1. 形成每日学习习惯
2. 让用户看到累计成长
3. 不增加系统复杂度

---

# 二、核心留存机制（只保留 3 个）

1. 连胜系统（Streak）
2. 累计统计
3. 每日记录

---

# 三、连胜系统（Streak System）

## 完成标准

满足以下任一条件视为完成当天学习：

- Dictation ≥ 3 句
或
- Shadowing ≥ 5 分钟

---

## 连胜逻辑

- 如果今天完成学习：
  currentStreak += 1
- 如果中断一天：
  currentStreak = 0
- maxStreak 保留历史最高值

---

## 数据结构

```ts
User {
  id: string
  email: string

  currentStreak: number
  maxStreak: number
  lastCompletedDate: string | null
}

四、累计统计系统

用于展示“成长感”。

统计内容
Totaldictationsentence
totaldictationminutes
totalShadowingSessions
totalShadowingSminutes

数据结构
UserStats {
  userId: string

  totalDictationSentences: number
  totalShadowingMinutes: number
  totalShadowingSessions: number
}

五、每日记录表（用于判断连胜）
DailyRecord {
  userId: string
  date: string

  dictationCount: number
  shadowingMinutes: number

  completed: boolean
}

六、数据更新时机
Dictation 完成一句时：

totalDictationSentences += 1

更新 DailyRecord.dictationCount

Shadowing 结束时：

totalShadowingMinutes += 本次分钟数

totalShadowingSessions += 1

更新 DailyRecord.shadowingMinutes

七、连胜判断逻辑

在每日练习结束时执行：
if (dictationCount >= 3 || shadowingMinutes >= 5) {
  completed = true
}

如果 completed = true：

若 lastCompletedDate 是昨天：
currentStreak += 1

若是今天重复完成：
不增加

若中断：
currentStreak = 1

更新 maxStreak。
八、Profile 页面展示（只显示 4 个数据）

🔥 当前连胜天数
📈 历史最高连胜
✍ 累计听写句数
🎙 累计 Shadowing 分钟

不展示 XP
不展示等级
不展示成就

保持极简。

十、完成标准

连胜计算准确

数据刷新后不丢失

Profile 实时更新

不影响现有 Dictation / Shadowing 功能

版本号v3


