📄 Dictation 时间统计规范

版本：V3.1
原则：数据库存秒，展示层转分钟

一、目标

本规范用于统一 Dictation（听写）练习时间统计逻辑。

✅ 精准记录真实有效作答时间

✅ 数据库存储单位为“秒”

✅ 展示层统一转换为“分钟”

✅ 防止挂机与异常数据

✅ 兼容两种模式（填空 / 整句）

✅ 可长期扩展（成长系统 / 周报 / 等级系统）

二、适用范围

本规范适用于：

Dictation - Word Mode（填空模式）

Dictation - Whole Caption Mode（整句模式）

不适用于 Shadowing（另有规范）

三、核心定义
1️⃣ 有效作答时间（Effective Practice Time）

定义为：

用户真实参与听写思考与输入的时间总和（单位：秒）

计算方式：
有效时间 = 总计时 - 暂停时间


四、计时启动规则

满足以下任一条件即开始计时：

用户第一次点击播放音频

用户输入框获得焦点

用户开始输入字符

记录：
start_time = 当前时间戳

仅第一次触发有效。


五、计时暂停规则

出现以下情况必须暂停计时：

1️⃣ 浏览器标签页失焦（visibilitychange）
2️⃣ 页面最小化
3️⃣ 连续无操作超过 60 秒

暂停逻辑：
pause_start = 当前时间

恢复时：
paused_duration += 当前时间 - pause_start


六、计时结束规则

当用户：

点击 Check answer
键盘回车键
点击 Show Words

立即停止计时。
end_time = 当前时间戳

七、时间计算方式

raw_seconds = end_time - start_time - paused_duration
然后进行有效性校验：
duration_seconds = 修正后的有效秒数


八、有效性过滤规则（防刷机制）

为避免数据异常，必须增加以下限制：
1️⃣ 最小有效时间
duration_seconds < 3 秒
→ 不计入统计
避免误触或快速跳过。

2️⃣ 最大单句上限
最大统计时间 = min(180秒, 音频时长 × 5)
防止：
挂机
故意拖延
数据异常膨胀

3️⃣ 异常截断规则

如果：
raw_seconds > 最大上限
则：
duration_seconds = 最大上限

九、播放时间的处理

播放时间：
play_seconds
可单独记录，用于数据分析，但：

听写总时间不直接等于播放时间。

十、数据库结构（Supabase）
表：dictation_events
create table dictation_events (
  id uuid primary key default gen_random_uuid(),

  user_id uuid references auth.users(id) on delete cascade,
  sentence_id uuid,

  mode text check (mode in ('word', 'whole')),

  duration_seconds integer not null,
  play_seconds integer default 0,

  attempt_count integer default 1,

  created_at timestamp with time zone default now()
);

十一、Profile 页面统计逻辑

数据库统一存秒：
duration_seconds
Profile 查询：
select sum(duration_seconds)
from dictation_events
where user_id = ?

十二、展示层转换逻辑

前端统一转换为分钟：

方式一：整数分钟
const totalMinutes = Math.floor(totalSeconds / 60)
显示：
听写总时长：128 分钟

十三、模式统一原则

填空模式与整句模式：

使用同一套时间统计逻辑

不允许不同模式采用不同算法

不允许句数 × 固定时间

设计原则总结

必须遵守：

✅ 数据库存秒

✅ 展示转分钟

✅ 只统计有效作答时间

✅ 防止挂机

✅ 单位统一

禁止：

❌ 句数 × 固定时间

❌ 直接存分钟

❌ 页面停留时间直接计入

❌ 不做上限控制

最终核心原则

时间必须真实
数据必须精确
单位必须统一
展示可以友好
